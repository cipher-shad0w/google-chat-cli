[project]
name = "gchat-tui"
version = "0.1.0"
description = "TUI for gchat CLI"
requires-python = ">=3.10"
dependencies = [
    "textual>=0.47.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["tui"]
